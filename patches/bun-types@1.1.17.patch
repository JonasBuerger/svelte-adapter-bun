diff --git a/bun.d.ts b/bun.d.ts
index 2649617417d6ed4d9388eb7f50d087de1eda4778..ed38785735bde6883ae486148b383f3914551695 100644
--- a/bun.d.ts
+++ b/bun.d.ts
@@ -115,6 +115,7 @@ declare module "bun" {
 		| ReadableStream;
 
 	class ShellError extends Error implements ShellOutput {
+		bytes(): Uint8Array;
 		readonly stdout: Buffer;
 		readonly stderr: Buffer;
 		readonly exitCode: number;
diff --git a/globals.d.ts b/globals.d.ts
index 7837840e362085fbd9a0b38b15fe3604e0f2132c..7a0fa32b96aa4d361d62f9df34057f5311b119a3 100644
--- a/globals.d.ts
+++ b/globals.d.ts
@@ -148,6 +148,7 @@ import type { WebSocket as _WebSocket } from "ws";
 
 declare module "*.txt" {
 	var text: string;
+	// @ts-ignore
 	export = text;
 }
 
@@ -1849,6 +1850,7 @@ declare global {
 		 * import.meta.env === process.env
 		 * ```
 		 */
+		// @ts-ignore
 		readonly env: NodeJS.ProcessEnv;
 
 		/**
@@ -1890,10 +1892,10 @@ declare global {
 		readonly main: boolean;
 
 		/** Alias of `import.meta.dir`. Exists for Node.js compatibility */
-		readonly dirname: string;
+		dirname: string;
 
 		/** Alias of `import.meta.path`. Exists for Node.js compatibility */
-		readonly filename: string;
+		filename: string;
 	}
 
 	/**
diff --git a/overrides.d.ts b/overrides.d.ts
index 46a1c2cb36e62b25e2004dc639ee4257a2bdb100..e0fb2c28ccb6691f519e2f700a2ff72275fbe0ba 100644
--- a/overrides.d.ts
+++ b/overrides.d.ts
@@ -1,6 +1,7 @@
 export {};
 
-import type { Env, PathLike, BunFile } from "bun";
+import type { Env, BunFile } from "bun";
+import type { PathLike } from "fs";
 
 declare global {
 	namespace NodeJS {
